{
  "name": "jsonserver",
  "version": "1.0.0",
  "description": "Fake REST API for BP-Tracker",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 4000"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
